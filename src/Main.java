import java.io.FileWriter;
import java.util.*;

public class Main {
    public static void main(String[] args) {

        //Scanner scanner = new Scanner(System.in);

        UserInput game = new UserInput();
        game.runGame();

        //String str = "";
        //Random random = new Random();
        //while (str.length() < 4) {
        //    String randomNum = random.nextInt(10) + "";
//
        //    if (!str.contains(randomNum)) {
        //        str += randomNum;
        //    }
//
        //}

        //BullsNCowsLogic bullsNCowsLogic = new BullsNCowsLogic();
        //String str = bullsNCowsLogic.randomHiddenNumber();
        //int countBulls = 0;

        //char[] hiddenNum = str.toCharArray();

       //while (countBulls != 4) {
       //    System.out.println("Введите число: ");
           //String inputStr = scanner.nextLine();
       //    countBulls = 0;
       //    int countCow = 0;
           //char[] num = inputStr.toCharArray();
        //System.out.println(bullsNCowsLogic.checkStep(num));
            //for (int i = 0; i < str.length(); i++) {
            //    for (int j = 0; j < str.length(); j++) {
            //        if (hiddenNum[i] == num[j] && i != j) {
            //            countCow++;
            //        }
            //    }
            //    if (hiddenNum[i] == num[i]) {
            //        countBulls++;
            //    }
            //}

           // System.out.println(inputStr);
           // System.out.println("Быков " + countBulls);
           // System.out.println("Коров " + countCow);
        // }
        //System.out.println(str);
    }

}